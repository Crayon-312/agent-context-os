# 任务报告：<任务名称>

## 已完成

<一句话说明完成了什么。>

## 已读上下文

- `AGENTS.md`
- `.agent-context/config.json`
- `<当前 Agent 协作层规则 / 项目记忆源 / 代码路径>`

## 修改范围

- `<路径>`：`<说明>`

## 变更等级

- 等级：`<S0 / S1 / S2 / S3>`
- 原因：`<为什么是这个等级>`
- 是否触发升级：`<否 / 升级原因>`

## 项目记忆源

- 检索状态：`<未需要 / 已检索 / 索引缺失改读源文件>`
- 命中摘要：`<无 / 相关记忆摘要>`
- 写回状态：`<无 / 新增 / 更新 / 废弃 / 校验>`
- 写回位置：`<无 / Obsidian Vault 路径 / JSONL 兼容源 / 业务文档 / 模块文档 / 问题库>`
- 冲突或过期：`<无 / 说明>`

## 本地索引

- 状态：`<未启用 / 有效 / 缺失 / 过期 / 已重建 / 需重建>`
- 位置：`<无 / 本机缓存 / .agent-context/local-index/>`
- Git 状态：`<未跟踪 / 已排除 / 风险说明>`

## 工作区模式

- 模式：`<主工作区 / Git worktree>`
- 原因：`<为什么使用或不使用 Git worktree>`
- worktree 路径：`<无 / 绝对路径>`
- worktree 分支：`<无 / codex/task-id>`
- 清理状态：`<not_required / pending / removed / pruned / blocked>`

## 验证结果

| 验证 | 结果 | 说明 |
|---|---|---|
| `<命令或方式>` | `<通过/失败/未运行>` | `<说明>` |

## 强检查结果

- 状态：`<通过 / 失败 / 跳过>`
- 已运行：`<scripts/check-agent-strong.ps1 / scripts/check-agent.ps1 / 其他命令>`
- 说明：`<失败、跳过或剩余风险说明>`

## 本地 Git 提交

- 状态：`<已提交 / 跳过 / 不适用>`
- 提交：`<commit hash / 跳过原因>`
- 是否推送（pushed）：`<是 / 否 / 未授权 / 不适用>`

## 方案状态

- 关联方案：`<无 / plan-id>`
- 方案阶段：`<无 / 讨论 / 草案记录 / 已定版 / 执行中>`
- 方案状态：`<无 / current / proposed / assumption / open / conflict>`
- 台账路径：`<无 / 当前 Agent 协作层台账路径>`
- 任务统计：`<总数 / 已完成 / 阻塞 / 延期 / 未开始>`

## 老项目升级

- 是否涉及旧 Agent 协作层清退：`<否 / 是>`
- 已提取项目记忆：`<无 / 摘要>`
- 已清退旧文件：`<无 / 路径>`
- 剩余旧结构：`<无 / 路径与原因>`

## 风险与后续

- `<风险>`
- `<后续建议>`

本地提交状态：`<已提交 / 跳过 / 不适用>`；是否推送（pushed）：`<是 / 否 / 未授权 / 不适用>`
