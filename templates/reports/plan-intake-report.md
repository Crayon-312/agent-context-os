# 方案摄取报告：<方案名称>

## 1. 方案来源

- 来源：`<用户输入 / 文档路径 / 对话摘要>`
- 方案类型：`<业务 / 产品 / 技术 / 执行计划>`
- 方案阶段：`<讨论 / 草案记录 / 已定版 / 执行中>`
- 方案状态：`<current / proposed / assumption / open / conflict>`
- 摄取确认：`<已确认 / 仅 discussion_only / 记录草案>`
- 方案落实台账：`<无 / 当前 Agent 协作层台账路径>`

## 2. 方案意图

- 业务初衷：`<为什么提出这个方案>`
- 要解决的问题：`<痛点>`
- 成功标准：`<如何判断方案有效>`
- 非目标：`<本方案不解决什么>`

## 3. 业务意义

| 类型 | 内容 | 状态 |
|---|---|---|
| 用户角色 | `<内容>` | `<状态>` |
| 业务对象 | `<内容>` | `<状态>` |
| 业务流程 | `<内容>` | `<状态>` |
| 字段规则 | `<内容>` | `<状态>` |
| 状态变化 | `<内容>` | `<状态>` |
| 验收口径 | `<内容>` | `<状态>` |

## 4. 实现逻辑

| 类型 | 内容 | 状态 |
|---|---|---|
| 模块影响 | `<内容>` | `<状态>` |
| 接口影响 | `<内容>` | `<状态>` |
| 数据影响 | `<内容>` | `<状态>` |
| 权限影响 | `<内容>` | `<状态>` |
| 配置影响 | `<内容>` | `<状态>` |
| 项目记忆源影响 | `<内容>` | `<状态>` |
| 验证影响 | `<内容>` | `<状态>` |

## 5. 系统影响矩阵

| 影响类型 | 影响内容 | 风险 | 写回位置 |
|---|---|---|---|
| `<类型>` | `<内容>` | `<风险>` | `<项目记忆源 / 文档路径 / 无>` |

## 6. 开发顺序与任务清单

| 顺序 | 任务 ID | 任务 | 来源摘要 | 依赖 | 验收口径 | 状态 |
|---|---|---|---|---|---|---|
| `<顺序>` | `<T1>` | `<任务>` | `<结论摘要>` | `<依赖>` | `<验收>` | `<draft / pending>` |

## 7. 冲突与开放问题

- 冲突：`<无 / 说明>`
- 开放问题：`<无 / 问题>`

## 8. 融合更新

- 写入当前 Agent 协作层方案索引：`<是 / 否>`
- 写入当前 Agent 协作层方案台账：`<是 / 否>`
- 写入项目记忆源：`<是 / 否>`
- 本地索引更新：`<无需 / 需增量更新 / 需重建>`
- 写入业务文档：`<无 / 路径>`
- 写入模块文档：`<无 / 路径>`
- 写入决策记录：`<无 / 路径>`
- 讨论阶段处理：`<不适用 / 仅在对话中整理，未修改项目记忆源>`

## 9. 后续任务

- `<任务>`
