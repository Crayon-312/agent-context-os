# 开发实现说明：<任务名称>

## 1. 修改目标

<本次要解决什么问题。>

## 2. 非目标

- `<本次明确不做什么>`

## 3. 变更等级

- 等级：`<S0 / S1 / S2 / S3>`
- 原因：`<为什么是这个等级>`
- 是否触发升级：`<否 / 升级原因>`

## 4. 项目记忆源引用

- 关联记忆：`<无 / Obsidian Vault 路径 / JSONL 兼容源 / 业务文档 / 模块文档>`
- 本地索引：`<未启用 / 有效 / 缺失 / 过期 / 需重建>`
- 证据路径：`<代码路径 / 测试路径 / 用户确认>`

## 5. 当前实现理解

<基于代码、测试、项目记忆源和必要文档确认的当前实现。>

## 6. 核心方案

<说明最小实现路径。>

## 7. 影响范围

| 模块 | 影响 | 风险 |
|---|---|---|
| `<模块>` | `<影响>` | `<风险>` |

## 8. 数据流与状态流

```text
<输入> -> <处理> -> <状态变化> -> <输出>
```

## 9. 历史问题检查

- 是否命中历史问题：`是 / 否`
- 关联问题：`<项目记忆源 ID / 路径>`
- 防复发措施：`<测试 / checklist / 记忆源更新>`

## 10. 记忆源写回计划

- 是否产生新项目事实或业务事实：`是 / 否`
- 是否产生新交互、风格或开发习惯：`是 / 否`
- 写回位置：`<Obsidian Vault 路径 / JSONL 兼容源 / 业务文档 / 模块文档 / 问题库 / 无>`
- 本地索引：`<无需更新 / 需增量更新 / 需重建>`
- 开放问题：`<无 / 需要用户确认的问题>`

## 11. 方案引用

- 关联方案：`<无 / plan-id>`
- 方案状态：`<无 / current / proposed / assumption / open / conflict>`
- 方案意图：`<摘要>`
- 系统影响矩阵：`<摘要或路径>`

## 12. 验证矩阵

| 验证项 | 命令或方式 | 预期 |
|---|---|---|
| `<验证项>` | `<命令>` | `<预期>` |

## 13. 风险与回滚

- 风险：`<风险>`
- 回滚点：`<如何回滚>`
